var a: number = null;
var b: number = null;

// Using a ternary operator to check for null.
b = a ? a : 0;
console.log('b = a ? a : 0; // b = ' + b);

myVal = 5;
    b = a ? a : 0;
    console.log('b = a ? a : 0; // b = ' + b);
    
// Using a ternary operator to evaluate.
var result:number=5;
var myVal:number=5;
var result1:string;
result1 = (myVal == result) ? 'myVal and result both equal ' : 'values are not equal';
console.log(result1);